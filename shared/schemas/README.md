# Shared Schemas

Shared TypeScript contracts that can be used by the storefront and operational scripts live here.

Supabase Edge Functions may copy or import compatible schemas when the runtime boundary allows it.
